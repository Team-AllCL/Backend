apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-ci
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-ci
  template:
    metadata:
      labels:
        app: order-ci
    spec:
      containers:
        - name: order-ci
          image: 961341508965.dkr.ecr.ap-northeast-2.amazonaws.com/order-ci:latest
          ports:
          - containerPort: 8080
      imagePullSecrets:
        - name: ecr-registry-secret