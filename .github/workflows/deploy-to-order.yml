name: Deploy JAR to order-server via Bastion

on:
  push:
    branches: [develop, jenkins-test]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v3
        with:
          distribution: 'oracle'
          java-version: '17'
          
      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew
        
      - name: Build with Gradle (skip test)
        run: ./gradlew build -x test

      - name: Upload JAR to Bastion
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.BASTION_HOST }}
          username: ${{ secrets.BASTION_USER }}
          key: ${{ secrets.BASTION_KEY }}
          source: build/libs/*.jar
          target: /home/${{ secrets.BASTION_USER }}/deploy/

      - name: SSH into Bastion and deploy to order-server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.BASTION_HOST }}
          username: ${{ secrets.BASTION_USER }}
          key: ${{ secrets.BASTION_KEY }}
          script: |
            scp -i /home/ec2-user/order-server-keypair.pem -o StrictHostKeyChecking=no \
              /home/${{ secrets.BASTION_USER }}/*.jar \
              ${{ secrets.ORDER_USER }}@${{ secrets.ORDER_HOST }}:/home/${{ secrets.ORDER_USER }}/app.jar

            ssh -i /home/ec2-user/order-server-keypair.pem -o StrictHostKeyChecking=no \
              ${{ secrets.ORDER_USER }}@${{ secrets.ORDER_HOST }} << 'ENDSSH'
              pkill -f 'java -jar' || true
              nohup java -jar /home/${{ secrets.ORDER_USER }}/app.jar > /home/${{ secrets.ORDER_USER }}/app.log 2>&1 &
            ENDSSH

